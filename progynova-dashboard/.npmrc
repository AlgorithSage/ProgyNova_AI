# Always install devDependencies (vite, typescript, eslint, etc.) even when the
# shell has NODE_ENV=production set, which otherwise makes npm omit them and
# breaks `npm run dev` / `tsc`. `include=dev` overrides that implicit omit.
include=dev
